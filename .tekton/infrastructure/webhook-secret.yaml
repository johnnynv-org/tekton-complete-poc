apiVersion: v1
kind: Secret
metadata:
  name: github-webhook-secret
  namespace: tekton-pipelines
type: Opaque
data:
  # Base64 encoded webhook secret
  # Replace 'your-webhook-secret-here' with actual secret
  webhook-secret: eW91ci13ZWJob29rLXNlY3JldC1oZXJl  # 'your-webhook-secret-here' in base64
---
# Example: To create a real secret, use:
# kubectl create secret generic github-webhook-secret \
#   --from-literal=webhook-secret=your-actual-secret \
#   -n tekton-pipelines
